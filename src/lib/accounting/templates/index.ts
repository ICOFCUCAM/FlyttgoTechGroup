// Phase 4 fills real chart-of-accounts content. This module just declares
// the exports that the seeder and chart browser depend on so that Phase 3
// compiles in isolation.
export { CHART_TEMPLATES } from './chart-templates';
export type { ChartAccountTemplate } from './chart-templates';
